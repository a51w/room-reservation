// Next.js requires this exact file (page.tsx) at this exact path to register the
// /admin/users route - see page-users.tsx in this same folder for the real code.
export { default } from "./page-users";
