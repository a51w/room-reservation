// Next.js requires this exact file (page.tsx) at this exact path to register the
// /admin/users route - see src/app/admin/page-users.tsx for the real code.
export { default } from "@/app/admin/page-users";
