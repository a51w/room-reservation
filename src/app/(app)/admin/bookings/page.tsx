// Next.js requires this exact file (page.tsx) at this exact path to register the
// /admin/bookings route - see page-booking.tsx in this same folder for the real code.
export { default } from "./page-booking";
