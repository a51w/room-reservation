// Next.js requires this exact file (page.tsx) at this exact path to register the
// /status route - see src/app/admin/page-rename.tsx for the real code (misnamed
// during the rename - it's actually the Room Status page, not anything about renaming).
export { default } from "@/app/admin/page-rename";
