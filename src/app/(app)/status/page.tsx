// Next.js requires this exact file (page.tsx) at this exact path to register the
// /status route - see page-status.tsx in this same folder for the real code.
export { default } from "./page-status";
