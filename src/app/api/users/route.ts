// Next.js requires this exact file (route.ts) at this exact path to register the
// GET /api/users handler - see route-users.ts in this same folder for the real code.
export { GET } from "./route-users";
