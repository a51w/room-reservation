// Next.js requires this exact file (route.ts) at this exact path to register the
// DELETE /api/bookings/[bookingId] handler - see route-bookingID.ts in this same
// folder for the real code.
export { DELETE } from "./route-bookingID";
