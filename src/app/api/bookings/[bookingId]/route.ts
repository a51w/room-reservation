// Next.js requires this exact file (route.ts) at this exact path to register the
// DELETE /api/bookings/[bookingId] handler - see @/app/api/route-bookingID.ts for
// the real code.
export { DELETE } from "@/app/api/route-bookingID";
