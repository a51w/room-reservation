// Next.js requires this exact file (route.ts) at this exact path to register the
// POST /api/register handler - see @/app/api/route-register.ts for the real code.
export { POST } from "@/app/api/route-register";
