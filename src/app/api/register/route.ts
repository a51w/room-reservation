// Next.js requires this exact file (route.ts) at this exact path to register the
// POST /api/register handler - see route-register.ts in this same folder for the real code.
export { POST } from "./route-register";
